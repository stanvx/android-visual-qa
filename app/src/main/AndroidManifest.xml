<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.BIND_ACCESSIBILITY_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- dataSync: M2 uses accessibility capture (not media projection). Change
         to mediaProjection or specialUse if a future lane switches to
         MediaProjectionManager for the capture source. -->

    <application
        android:label="Android Visual QA"
        android:theme="@style/Theme.AndroidVisualQa"
        android:allowBackup="false">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".trigger.QuickSettingsTile"
            android:label="Capture feedback"
            android:icon="@drawable/ic_capture_feedback"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
            android:exported="true"
            android:process=":a11y">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- M2 bridge: registers the service instance into AppServiceRegistry.
             In M3, replace with proper DI or binding. -->
        <service
            android:name=".BridgeAccessibilityService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:process=":a11y">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- M2 foreground service that owns the capture → draft → editor flow.
             Exported so ADB can drive the flow during testing; the QS tile
             starts it via startForegroundService() in the same UID.
             Runs in the :a11y process so it can see the bridge service via
             the in-process AppServiceRegistry. -->
        <service
            android:name=".CaptureForegroundService"
            android:exported="true"
            android:foregroundServiceType="dataSync"
            android:process=":a11y" />

        <!-- M3 FileProvider for export share sheet and draft access. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
