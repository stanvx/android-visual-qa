package com.androidvisualqa.capture.api

/** Identifies how the user initiated a capture. */
public enum class TriggerSource {
    AccessibilityBubble,
    QuickSettingsTile,
    NotificationAction,
    HardwareShortcut,
}
