package com.androidvisualqa.sdk.composecore

/**
 * Re-export of [com.androidvisualqa.privacy.Sensitivity] so SDK consumers
 * do not need a direct dependency on the [:core:privacy] module.
 */
typealias PrivacyClassification = com.androidvisualqa.privacy.Sensitivity

/**
 * Alternative import-friendly alias matching the SDK naming convention.
 */
typealias SdkSensitivity = com.androidvisualqa.privacy.Sensitivity
